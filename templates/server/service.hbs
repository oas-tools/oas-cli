{{#if options.esm}}
export function {{opId}}(req, res) {
    res.send({
        message: 'This is the mockup controller for {{opId}}'
    });
}

{{else}}
module.exports.{{opId}} = function {{opId}}(req, res) {
    res.send({
        message: 'This is the mockup controller for {{opId}}'
    });
}

{{/if}}